kind = "web"
previewPath = "/"
title = "TradeX Trading Platform"
version = "1.0.0"
id = "artifacts/tradex"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24753

[services.development]
run = "pnpm --filter @workspace/tradex run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tradex", "run", "build" ]
publicDir = "artifacts/tradex/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24753"
BASE_PATH = "/"
